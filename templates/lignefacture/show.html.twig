{% extends 'base.html.twig' %}

{% block title %}Lignefacture{% endblock %}

{% block body %}
    <h1>Lignefacture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lignefacture.id }}</td>
            </tr>
            <tr>
                <th>Qte</th>
                <td>{{ lignefacture.qte }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ lignefacture.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lignefacture_index') }}">back to list</a>

    <a href="{{ path('lignefacture_edit', {'id': lignefacture.id}) }}">edit</a>

    {{ include('lignefacture/_delete_form.html.twig') }}
{% endblock %}
